<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
    </PropertyGroup>

    <!--
      The innermost layer: entities, value objects and domain rules.
      It intentionally has no package or project references, so domain logic
      cannot take a dependency on EF Core, ASP.NET Core or any other framework.
    -->

</Project>
